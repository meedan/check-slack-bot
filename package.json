{
  "name": "check-slack-bot",
  "version": "0.0.1",
  "description": "A Check bot for Slack",
  "main": "index.js",
  "dependencies": {
    "lokka": "^1.7.0",
    "lokka-transport-http": "^1.6.1",
    "util": "^0.10.3"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && rm package.zip && zip -r package.zip * && echo \"----------\" && echo \"Now upload package.zip to AWS Lambda\""
  },
  "author": "Meedan",
  "license": "MIT"
}
